1) expresion regular y entrega un AFN (thompson)
2) expresion regular y entrega un AFD (arbol)



###COMPILAR ARCHIVOS .gv
	$ dot -Tpng archivo.gv > archivo.png
		->archivo.png


###COMPILAR ARCIVOS .l
	$ lex archivo.l
		->lex.yy.c
	$ gcc -Wall lex.yy.c -lfl -o archivo
		->archivo (ejecutable)


###COMPILAR ARCHIVOS .y Y .l
	
	***LEX***
	$ lex archivo.l
		->lex.yy.c
	$ gcc -Wall lex.yy.c -c
		->lex.yy.o

	***BISON***
	$ bison archivo.y -d
		->archivo.tab.h
	$ gcc -Wall archivo.tab.c -c
		->archivo.tab.o

	***COMPILACION***
	$ gcc -Wall lex.yy.o archivo.tab.o -lm -lfl -o archivo
		->archivo (ejecutable)